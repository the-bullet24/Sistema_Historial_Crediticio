server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres?currentSchema=core
    username: postgres.eqsaxdoalxvvltdumptt
    password: SistemaScore123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: core
        database-platform: org.hibernate.dialect.PostgreSQLDialect

  jackson:
    serialization:
      fail-on-empty-beans: false

logging:
  level:
    org.hibernate.SQL: DEBUG

# ── JWT ────────────────────────────────────────────────────────────────────────
# DESARROLLO: clave hardcodeada para facilitar las pruebas locales.
# PRODUCCIÓN: reemplazar por variable de entorno ${JWT_SECRET}
#             y generar clave con: openssl rand -base64 32
app:
  jwt:
    secret: U2NvcmVDcmVkaXRTZWNyZXRLZXkyMDI2U2VjcmV0S2V5MjAyNg==
    expiration: 86400000